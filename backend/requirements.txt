# Production runtime dependencies only. Keep this list minimal — every
# extra package here adds to the deployed container's memory footprint
# (relevant on memory-capped hosts like Render's 512Mi free/starter tier).
# See requirements-dev.txt for test/lint-only tooling.
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.10.0
anthropic>=0.40.0
python-dotenv>=1.0.0
cachetools>=5.3.0
