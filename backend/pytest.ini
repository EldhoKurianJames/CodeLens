[pytest]
# smoke_test.py makes real HTTP calls against a locally running server at
# import time (not inside test functions), so it can't be part of the
# automated suite / CI run — it's a manual smoke-check script. See its
# docstring / README for how to run it directly with `python tests/smoke_test.py`.
addopts = --ignore=tests/smoke_test.py
